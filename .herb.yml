version: 0.10.3

files:
  exclude:
    - "vendor/**/*"
    - "tmp/**/*"
    - "node_modules/**/*"
    - "public/**/*"

linter:
  enabled: true
  failLevel: error

  rules:
    # Partials use local_assigns.key? pattern, not strict locals yet — warn, not error
    erb-strict-locals-required:
      enabled: true
      severity: warning

    html-img-require-alt:
      enabled: true
      severity: error

    html-tag-name-lowercase:
      enabled: true
      severity: error

    html-require-closing-tags:
      enabled: true
      severity: error

    parser-no-errors:
      enabled: true
      severity: error
    actionview-no-silent-helper:
      enabled: true
      severity: error
    erb-no-output-in-attribute-name:
      enabled: true
      severity: error
    erb-prefer-direct-output:
      enabled: true
      severity: error

formatter:
  enabled: false
