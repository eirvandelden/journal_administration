%nav.secondary
  %ul
    %li= link_to 'Edit', edit_category_group_path(@category_group)
    %li= link_to 'Back', category_groups_path

= render partial: 'form', locals: { disabled: :disabled }

%section
  = link_to @category_group, method: :delete, data: { confirm: 'Are you sure?' } do
    %b Destroy
