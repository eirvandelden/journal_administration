<header>
  <h1><%= @chattel.name %></h1>
  <nav>
    <%= link_to @chattel do %>
      <b><%= t('common.show') %></b>
    <% end %>
    <%= button_to t('common.destroy'), @chattel, method: :delete, data: { turbo_confirm: t('common.confirm_destroy') } %>
  </nav>
</header>
<article>
  <%= render 'form' %>
</article>
