<% content_for(:title) { t(".title") } %>
<header>
  <h1><%= t('.title') %></h1>
  <nav>
    <%= link_to t('common.back'), chattels_path %>
  </nav>
</header>
<article>
  <%= render 'form', chattel: @chattel, brands_in_use: @brands_in_use %>
</article>
