<%# locals: (account:, account_alias:) %>
<%= form_with model: [account, account_alias] do |f| %>
  <%= f.text_field :pattern, placeholder: t("account_aliases.form.pattern_placeholder"),
        required: true %>
  <%= f.submit t("account_aliases.form.submit") %>
<% end %>
