%h1
  = @category.name
  = link_to @category do
    %b= t('common.show')
  = link_to @category, method: :delete, data: { confirm: t('common.confirm_destroy') } do
    %b= t('common.destroy')

= render 'form'
