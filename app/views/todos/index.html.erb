<article class="todo-list">
  <% if @empty %>
    <p><%= t('.empty') %></p>
  <% else %>
    <%= render "shared/pagination", page: @page %>
    <table class="todo">
      <thead>
        <tr>
          <th><%= t('.date') %></th>
          <th><%= t('.kind') %></th>
          <th><%= t('.description') %></th>
          <th><%= t('.actions') %></th>
        </tr>
      </thead>
      <tbody>
        <% @items.each do |item| %>
          <% kind = t(".#{item.kind}") %>
          <% actions = capture do %>
            <% case item.kind %>
            <% when :transaction %>
              <%= link_to t(".edit"), edit_transaction_path(item.record) %>
            <% when :product %>
              <%= link_to t(".classify"), edit_product_path(item.record) %>
            <% else %>
              <%= link_to t(".edit"), edit_account_path(item.record) %>
              <%= button_to t(".update_transactions"), account_transactions_bulk_path(item.record), method: :patch %>
            <% end %>
          <% end %>
          <tr>
            <td><%= l item.date, format: :short %></td>
            <td><%= kind %></td>
            <td><%= todo_description(item) %></td>
            <td><%= actions %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= render "shared/pagination", page: @page %>
  <% end %>
</article>
